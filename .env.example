# Required for LLM-generated answers. Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: override path to folder containing PDFs (default: ./data)
# DOCUMENTS_PATH=./data

# Optional: ChromaDB persistence (default: ./chroma_db)
# CHROMA_PERSIST_DIR=./chroma_db
